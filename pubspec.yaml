name: test_cicd
description: "A new Flutter project."
publish_to: 'none'
version: 0.2.1

environment:
  sdk: ^3.8.0

dependencies:
  firebase_core: ^4.2.0
  flutter:
    sdk: flutter
  flutter_dotenv: ^6.0.0

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
